# Review: tasks 013/015 — batch-2

<!-- File: reviews/review-2026-03-21-5.md -->
<!-- Author: claude (orchestrator) -->

---

## Metadata

| Field | Value |
|---|---|
| Review ID | `review-2026-03-21-5` |
| Reviewing | Tasks `013`, `015` / Plan `plan-002 (batch-2)` |
| Invocation mode | 013 = Mode A (Codex), 015 = Claude direct |
| Report read | N/A (both Mode A / Claude direct) |
| Date | 2026-03-21 |
| Reviewer | claude |

---

## Summary

Batch-2 complete. `CLAUDE.md` now has explicit spec change rules, and `board.yaml` structure is clean with a single `current_plan` field.

---

## Acceptance criteria check

### Task 013 — add-spec-conflict-rules (Mode A)

| Criterion | Result | Notes |
|---|---|---|
| `CLAUDE.md` contains "Spec change rules" section | **pass** | Added at line ~52 |
| States Claude must set `spec_dirty: true` when modifying `SPEC.md` | **pass** | |
| States Claude must not invoke Codex while `spec_dirty: true` + tasks `in_progress` | **pass** | |
| Describes how to clear `spec_dirty` after impact evaluation | **pass** | |

### Task 015 — board-yaml-cleanup (Claude direct)

| Criterion | Result | Notes |
|---|---| ---|
| `board.yaml` has single `current_plan` field pointing to plan-002 | **pass** | Duplicate plan-001 block removed |
| No `next_plan` field in `board.yaml` | **pass** | Field eliminated |

---

## Decision

- [x] **Accepted** — batch-2 done.

---

## Next step recommendation

Batch-3 (optional template simplifications) remains. Items:
- Merge `Definition of done` into `Acceptance criteria` in task-template
- Simplify Mode B execution log to summary + report reference
- Merge `decision-template` + `changelog-template` into one file
- Make `Steps` section optional in task-template

All are low-risk, low-urgency. System is fully operational without them.
