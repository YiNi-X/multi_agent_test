# Review: tasks 005/006/007 — P0 protocol fixes

<!-- File: reviews/review-2026-03-21-1.md -->
<!-- Author: claude (orchestrator) -->

---

## Metadata

| Field | Value |
|---|---|
| Review ID | `review-2026-03-21-1` |
| Reviewing | Tasks `005`, `006`, `007` / Plan `plan-002 (P0 fixes)` |
| Reports read | `reports/task-005-fix-protocol-orphan-fields-2026-03-21.md`, `reports/task-006-fix-review-state-rule-2026-03-21.md`, `reports/task-007-fix-handoff-orchestrator-state-2026-03-21.md` |
| Date | 2026-03-21 |
| Reviewer | claude |

---

## Summary

All three P0 protocol fix tasks completed by Codex in a single session (`019d0ead-cadd-7341-86d0-877d215983ef`). All acceptance criteria verified by direct inspection of Codex reports. No rework required.

---

## Acceptance criteria check

### Task 005 — fix-protocol-orphan-fields

| Criterion | Result | Notes |
|---|---|---|
| `codex-worker-protocol.md` contains rule to increment `total_tasks_executed` when `last_task_id` updated | **pass** | Added to session lifecycle diagram and as explicit rule at line 56 |
| `codex-worker-protocol.md` contains `spec_hash_at_start` write step with format `"<version>/<last_updated>"` | **pass** | Added under Method 1 before `codex()` call |
| `board.yaml` comment above `suggested_next_for_codex` states it is only valid when `current_plan.status == "in_progress"` | **pass** | Comment was already present from board.yaml edit; Codex confirmed no change needed |

### Task 006 — fix-review-state-rule

| Criterion | Result | Notes |
|---|---|---|
| `README.md` `review` description states `board.yaml` must be set to `review` before Claude starts writing review doc | **pass** | README.md line ~37 updated |
| `README.md` contains note that `review` is a durable state, not transient | **pass** | Explicit note added after status table |
| `task-template.md` has three separate steps: set `review` → write review doc → set `done` | **pass** | Template line ~89 split into three steps |

### Task 007 — fix-handoff-orchestrator-state

| Criterion | Result | Notes |
|---|---|---|
| `codex-handoff.md` contains `## Orchestrator state` section with current plan, spec version, last review, pending decisions, next action | **pass** | Populated from board.yaml and SPEC.md |
| `codex-worker-protocol.md` defines required two-section structure for `codex-handoff.md` | **pass** | Section added at end of protocol file |
| Four open questions from `SPEC.md` listed in `codex-handoff.md` | **pass** | Listed under "Open questions requiring resolution before next plan" |

---

## Observations

- Codex correctly refused the first invocation that bundled all three tasks, adhering to the single-task rule from AGENTS.md. This validates the protocol is working as designed.
- Task-007: Codex noted that the latest review on disk is `review-2026-03-18-1` (task-003), not task-004. This is correct — task-004 was accepted inline during the task-003 review document. The handoff reflects the actual file state.
- `total_tasks_executed` is now 3 in `codex-session.yaml`, correctly reflecting tasks 005, 006, 007 executed in this session.

---

## Decision

- [x] **Accepted** — all three tasks done. P0 complete.

---

## Next step recommendation

P0 is complete. Proceed to P1 architecture discussion:
1. **reviews/reports dual-mode documentation** — clarify when Codex writes reports vs. Claude reads MCP response directly
2. **Multi-session parallel schema** — extend `codex-session.yaml` to support multiple concurrent Codex sessions for independent tasks
3. **Session canary probe** — harden session expiry detection beyond time-based heuristics
